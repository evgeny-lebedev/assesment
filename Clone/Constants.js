// DOM Utils
const TEXT_ELEMENT = "TEXT ELEMENT";
const ON = "on";
const CHILDREN = "children";
const STYLE = "style";

// Fiber tags
const HOST_ROOT = 1;
const CLASS_COMPONENT = 2;
const HOST_COMPONENT = 3;

// Effect tags
const PLACEMENT = 1;
const DELETION = 2;
const UPDATE = 3;

const ENOUGH_TIME = 1;

export {
  TEXT_ELEMENT,
  ON, CHILDREN,
  STYLE,
  HOST_COMPONENT,
  CLASS_COMPONENT,
  HOST_ROOT,
  PLACEMENT,
  DELETION,
  UPDATE,
  ENOUGH_TIME
};